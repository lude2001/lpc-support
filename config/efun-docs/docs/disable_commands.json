{
  "name": "disable_commands",
  "summary": "让一个活着的对象变成非活着的状态，也就是说：add_action() 无效，living() 返回值为0，而且，如果对象是玩家，将变的无法使用任何指令（input_to()依然可用）。\n\n   调用 disable_commands() 函数还有一个附加效果是清理掉此对象的所有行为指令，包括此对象本身定义的和来自其它对象的。\n\n   注意：这个函数本质上改变了对象的特性，不要简单的理解为禁用命令功能，而是失去所有生物相关的能力。",
  "details": "Return semantics:\ndisable_commands()函数永远返回0",
  "reference": [
    "enable_commands"
  ],
  "category": "互动对象(玩家)相关函数（Interactive）",
  "signatures": [
    {
      "label": "int disable_commands()",
      "returnType": "int",
      "isVariadic": false,
      "parameters": [],
      "arity": {
        "min": 0,
        "max": 0
      }
    }
  ]
}
