{
  "name": "set_eval_limit",
  "summary": "set_eval_limit() with a nonzero argument, sets the maximum evaluation cost that is allowed for any one thread before a runtime error occurs. With a zero argument, it sets the current evaluation counter to zero, and the maximum cost is returned. set_eval_limit(-1) returns the remaining evaluation cost.",
  "reference": [
    "catch",
    "error",
    "throw",
    "error_handler",
    "eval_cost",
    "reset_eval_cost",
    "max_eval_cost"
  ],
  "category": "系统相关函数（System）",
  "signatures": [
    {
      "label": "int set_eval_limit( int )",
      "returnType": "int",
      "isVariadic": false,
      "parameters": [
        {
          "name": "arg1",
          "type": "int"
        }
      ],
      "arity": {
        "min": 1,
        "max": 1
      }
    }
  ]
}
