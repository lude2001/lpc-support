{
  "name": "reload_object",
  "summary": "当对象 `ob` 执行 reload_object()后，所有由驱动程序设置的属性全部初始化（heart_beat，call_outs，light，shadow等），所有变量也初始化，并且调用 create() 方法。这和销毁并重载对象等效果类似，但是，不会存取磁盘数据和执行语法分析，如果代码修改过，reload_object 不会载入新代码。",
  "reference": [
    "export_uid",
    "new",
    "clone_object",
    "destruct"
  ],
  "category": "对象相关函数（Objects）",
  "signatures": [
    {
      "label": "void reload_object( object ob )",
      "returnType": "void",
      "isVariadic": false,
      "parameters": [
        {
          "name": "ob",
          "type": "object"
        }
      ],
      "arity": {
        "min": 1,
        "max": 1
      }
    }
  ]
}
