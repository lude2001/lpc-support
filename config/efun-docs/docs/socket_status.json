{
  "name": "socket_status",
  "summary": "socket_status(int fd) returns an array with:\n   . ret[0] = (int) fd\n       . ret[1] = (string) state\n       . ret[2] = (string) mode\n       . ret[3] = (string) local address\n       . ret[4] = (string) remote address\n       . ret[5] = (object) owner\n   socket_status() returns an array of arrays; one for each socket.",
  "category": "SOCKET相关函数（Sockets）",
  "signatures": [
    {
      "label": "mixed *socket_status(void | int)",
      "returnType": "mixed *",
      "isVariadic": false,
      "parameters": [
        {
          "name": "int",
          "type": "void |",
          "optional": true
        }
      ],
      "arity": {
        "min": 0,
        "max": 1
      }
    }
  ]
}
