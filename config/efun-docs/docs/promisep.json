{
  "name": "promisep",
  "summary": "Returns 1 if `arg` is a promise, 0 otherwise. The `*p()` type test for the `promise` type, equivalent to `typeof(arg) == \"promise\"`.",
  "reference": [
    "promise_create",
    "promise_status",
    "promise_result",
    "typeof"
  ],
  "category": "Promise 与异步（Promise / Async）",
  "availability": {
    "package": "core",
    "condition": "始终启用的 PACKAGE_CORE",
    "source": "docs/efun/promises/promisep.md"
  },
  "signatures": [
    {
      "label": "int promisep(mixed arg)",
      "returnType": "int",
      "isVariadic": false,
      "parameters": [
        {
          "name": "arg",
          "type": "mixed"
        }
      ],
      "arity": {
        "min": 1,
        "max": 1
      }
    }
  ]
}
