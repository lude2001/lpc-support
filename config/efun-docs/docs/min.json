{
  "name": "min",
  "summary": "FluffOS 1.33新增efun，寻找数组 `arr` (int|float|string) 中的最小值，如果参数 `flag` 缺省或值为 0，函数返回最小值，否则返回第一个最小值的索引。",
  "reference": [
    "max"
  ],
  "category": "FluffOS新增函数（Contrib）",
  "signatures": [
    {
      "label": "mixed min(mixed * arr)",
      "returnType": "mixed",
      "isVariadic": false,
      "parameters": [
        {
          "name": "arr",
          "type": "mixed *"
        }
      ],
      "arity": {
        "min": 1,
        "max": 1
      }
    },
    {
      "label": "mixed min(mixed * arr, int flag)",
      "returnType": "mixed",
      "isVariadic": false,
      "parameters": [
        {
          "name": "arr",
          "type": "mixed *"
        },
        {
          "name": "flag",
          "type": "int"
        }
      ],
      "arity": {
        "min": 2,
        "max": 2
      }
    }
  ]
}
