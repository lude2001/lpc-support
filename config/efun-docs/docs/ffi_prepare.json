{
  "name": "ffi_prepare",
  "summary": "Resolves `name` in `lib`, builds the platform call frame for the described signature, and returns a callable function handle for ffi_call(). `ret_type` and each element of `arg_types` are type codes from <ffi.h> (FFI_INT32, FFI_DOUBLE, FFI_POINTER, ...).",
  "reference": [
    "ffi_call",
    "ffi_load",
    "ffi_sizeof",
    "valid_ffi"
  ],
  "category": "外部函数接口（FFI）",
  "availability": {
    "package": "ffi",
    "condition": "需要 PACKAGE_FFI 和 libffi；WebAssembly 目标不可用",
    "source": "docs/efun/ffi/ffi_prepare.md"
  },
  "signatures": [
    {
      "label": "int ffi_prepare( int lib, string name, int ret_type, int *arg_types )",
      "returnType": "int",
      "isVariadic": false,
      "parameters": [
        {
          "name": "lib",
          "type": "int"
        },
        {
          "name": "name",
          "type": "string"
        },
        {
          "name": "ret_type",
          "type": "int"
        },
        {
          "name": "arg_types",
          "type": "int *"
        }
      ],
      "arity": {
        "min": 4,
        "max": 4
      }
    }
  ]
}
