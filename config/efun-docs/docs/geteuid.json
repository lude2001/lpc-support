{
  "name": "geteuid",
  "summary": "如果参数是对象，返回对象的有效用户ID（euid），如果参数是一个函数指针，返回创建这个函数指针的对象的有效用户ID，如果在创建这个函数指针时对象没有 euid ，会使用对象的有效用户ID（uid）代替。如果不指定对象，默认对象为 this_object()。",
  "reference": [
    "seteuid",
    "getuid",
    "functionp",
    "export_uid",
    "previous_object",
    "this_object",
    "valid_seteuid"
  ],
  "category": "MUDLIB相关函数（Mudlib）",
  "signatures": [
    {
      "label": "string geteuid( object ob | function f | void)",
      "returnType": "string",
      "isVariadic": false,
      "parameters": [
        {
          "name": "f",
          "type": "object ob | function",
          "optional": true
        }
      ],
      "arity": {
        "min": 0,
        "max": 1
      }
    }
  ]
}
