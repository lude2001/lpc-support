{
  "name": "max",
  "summary": "FluffOS 1.33新增efun，在数组 `arr` (int|float|string) 中查找最大值，如果参数 `flag` 缺省或值为 0，函数返回最大值，否则返回第一个最大值的索引。",
  "reference": [
    "min"
  ],
  "category": "FluffOS新增函数（Contrib）",
  "signatures": [
    {
      "label": "mixed max(mixed * arr)",
      "returnType": "mixed",
      "isVariadic": false,
      "parameters": [
        {
          "name": "arr",
          "type": "mixed *"
        }
      ],
      "arity": {
        "min": 1,
        "max": 1
      }
    },
    {
      "label": "mixed max(mixed * arr, int flag)",
      "returnType": "mixed",
      "isVariadic": false,
      "parameters": [
        {
          "name": "arr",
          "type": "mixed *"
        },
        {
          "name": "flag",
          "type": "int"
        }
      ],
      "arity": {
        "min": 2,
        "max": 2
      }
    }
  ]
}
