{
  "name": "ffi_load",
  "summary": "Opens the shared library at `path` (via dlopen/LoadLibrary) and returns a positive library handle for use with ffi_symbol() and ffi_prepare(). Returns 0 on failure; call ffi_error() for the reason.",
  "reference": [
    "ffi_symbol",
    "ffi_prepare",
    "ffi_unload",
    "ffi_error",
    "valid_ffi"
  ],
  "category": "外部函数接口（FFI）",
  "availability": {
    "package": "ffi",
    "condition": "需要 PACKAGE_FFI 和 libffi；WebAssembly 目标不可用",
    "source": "docs/efun/ffi/ffi_load.md"
  },
  "signatures": [
    {
      "label": "int ffi_load( string path )",
      "returnType": "int",
      "isVariadic": false,
      "parameters": [
        {
          "name": "path",
          "type": "string"
        }
      ],
      "arity": {
        "min": 1,
        "max": 1
      }
    }
  ]
}
