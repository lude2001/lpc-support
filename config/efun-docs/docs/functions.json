{
  "name": "functions",
  "summary": "函数返回指定对象 `ob` 中定义的局部函数（方法）信息(包括继承对象中的方法)，可选参数 `flag` 为位域标识符，如果不指定默认为 0，表示只返回函数名称列表，如果指定为 1，返回一个二维数组，子数组格式为：\n\n   ({ 函数名, 参数个数, 返回值类型, 参数类型 })\n\n   如果`flag`为2，返回结果和`flag`为0类似，如果`flag`为3，返回结果和`flag`为1类似，区别是结果中不包括继承的函数。",
  "reference": [
    "variables"
  ],
  "category": "FluffOS新增函数（Contrib）",
  "signatures": [
    {
      "label": "mixed *functions( object ob )",
      "returnType": "mixed *",
      "isVariadic": false,
      "parameters": [
        {
          "name": "ob",
          "type": "object"
        }
      ],
      "arity": {
        "min": 1,
        "max": 2
      }
    },
    {
      "label": "mixed *functions( object ob, int flag )",
      "returnType": "mixed *",
      "isVariadic": false,
      "parameters": [
        {
          "name": "ob",
          "type": "object"
        },
        {
          "name": "flag",
          "type": "int",
          "optional": true
        }
      ],
      "arity": {
        "min": 1,
        "max": 2
      }
    }
  ]
}
