{
  "name": "ffi_sizeof",
  "summary": "Returns the size in bytes, on this platform, of the scalar C type named by `type_code` (a code from <ffi.h>). Useful for sizing ffi_alloc() blocks and computing offsets by hand.",
  "reference": [
    "ffi_alloc",
    "ffi_struct_layout",
    "ffi_read"
  ],
  "category": "外部函数接口（FFI）",
  "availability": {
    "package": "ffi",
    "condition": "需要 PACKAGE_FFI 和 libffi；WebAssembly 目标不可用",
    "source": "docs/efun/ffi/ffi_sizeof.md"
  },
  "signatures": [
    {
      "label": "int ffi_sizeof( int type_code )",
      "returnType": "int",
      "isVariadic": false,
      "parameters": [
        {
          "name": "type_code",
          "type": "int"
        }
      ],
      "arity": {
        "min": 1,
        "max": 1
      }
    }
  ]
}
