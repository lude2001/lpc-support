{
  "name": "seteuid",
  "summary": "设置当前对象的有效用户ID为 `id`。主控对象中的 valid_seteuid() 方法控制了对象的 euid 可以被设置为什么。\n\n   如果 euid 设置为 0，当前对象的用户ID（uid）可以且仅可以使用 export_uid() 外部函数改变。\n\n   但是，如果 euid 为 0，这个对象不能载入或复制任何对象。",
  "reference": [
    "export_uid",
    "getuid",
    "geteuid",
    "this_object",
    "valid_seteuid"
  ],
  "category": "MUDLIB相关函数（Mudlib）",
  "signatures": [
    {
      "label": "int seteuid( string | int id )",
      "returnType": "int",
      "isVariadic": false,
      "parameters": [
        {
          "name": "id",
          "type": "string | int"
        }
      ],
      "arity": {
        "min": 1,
        "max": 1
      }
    }
  ]
}
