{
  "name": "find_cycles",
  "summary": "Returns one index path per back-edge -- per slot that closes a reference loop in `value`. An empty array means the value is acyclic. Breaking (or deleting) exactly the returned slots would make the value loop-free; break_cycles() does that in one call.",
  "reference": [
    "has_cycle",
    "break_cycles",
    "refs"
  ],
  "category": "引用环（Reference Cycles）",
  "availability": {
    "package": "contrib",
    "condition": "需要 PACKAGE_CONTRIB（官方默认启用）",
    "source": "docs/efun/contrib/find_cycles.md"
  },
  "signatures": [
    {
      "label": "string *find_cycles(mixed value)",
      "returnType": "string *",
      "isVariadic": false,
      "parameters": [
        {
          "name": "value",
          "type": "mixed"
        }
      ],
      "arity": {
        "min": 1,
        "max": 1
      }
    }
  ]
}
